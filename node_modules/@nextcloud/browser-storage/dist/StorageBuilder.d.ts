import type Storage from './Storage.ts';
export default class StorageBuilder {
    private appId;
    private persisted;
    private clearedOnLogout;
    constructor(appId: string);
    persist(persist?: boolean): StorageBuilder;
    clearOnLogout(clear?: boolean): StorageBuilder;
    build(): Storage;
}
