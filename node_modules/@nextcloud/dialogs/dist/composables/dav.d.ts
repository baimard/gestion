import { IFolder, INode } from '@nextcloud/files';
import { ComputedRef, Ref } from 'vue';
/**
 * Handle file loading using WebDAV
 *
 * @param currentView Reference to the current files view
 * @param currentPath Reference to the current files path
 */
export declare function useDAVFiles(currentView: Ref<'files' | 'recent' | 'favorites'> | ComputedRef<'files' | 'recent' | 'favorites'>, currentPath: Ref<string> | ComputedRef<string>): {
    isLoading: Ref<boolean, boolean>;
    files: Ref<INode[], INode[]>;
    folder: import('vue').ShallowRef<IFolder | null, IFolder | null>;
    loadFiles: () => Promise<void>;
    createDirectory: (name: string) => Promise<IFolder>;
};
