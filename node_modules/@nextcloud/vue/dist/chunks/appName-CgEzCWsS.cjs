"use strict";
const initialState = require("@nextcloud/initial-state");
const Vue = require("vue");
const logger = require("./logger-3HuiEIF6.cjs");
/*!
 * SPDX-FileCopyrightText: 2025 Nextcloud GmbH and Nextcloud contributors
 * SPDX-License-Identifier: AGPL-3.0-or-later
 */
function once(func) {
  let wasCalled = false;
  let result;
  return (...args) => {
    if (!wasCalled) {
      wasCalled = true;
      result = func(...args);
    }
    return result;
  };
}
/*!
 * SPDX-FileCopyrightText: 2025 Nextcloud GmbH and Nextcloud contributors
 * SPDX-License-Identifier: AGPL-3.0-or-later
 */
let realAppName = "missing-app-name";
try {
  realAppName = appName;
} catch {
  logger.logger.error("The `@nextcloud/vue` library was used without setting / replacing the `appName`.");
}
const APP_NAME = realAppName;
let realAppVersion = "";
try {
  realAppVersion = appVersion;
} catch {
  logger.logger.error("The `@nextcloud/vue` library was used without setting / replacing the `appVersion`.");
}
const APP_VERSION = realAppVersion;
function useAppName() {
  return Vue.inject("appName", APP_NAME);
}
const useLocalizedAppName = once(() => {
  const apps = initialState.loadState("core", "apps", []);
  const realAppName2 = useAppName();
  return apps.find(({ id }) => id === realAppName2)?.name ?? realAppName2;
});
exports.APP_VERSION = APP_VERSION;
exports.useAppName = useAppName;
exports.useLocalizedAppName = useLocalizedAppName;
//# sourceMappingURL=appName-CgEzCWsS.cjs.map
