"use strict";
const mdi = require("./mdi-DTiP6a6e.cjs");
const core = require("@vueuse/core");
const Vue = require("vue");
const _l10n = require("./_l10n-skrZri3h.cjs");
_l10n.register(_l10n.t21);
/*!
 * SPDX-FileCopyrightText: 2025 Nextcloud GmbH and Nextcloud contributors
 * SPDX-License-Identifier: AGPL-3.0-or-later
 */
const DELAY = 2e3;
function useCopy(content) {
  const isCopied = Vue.ref(false);
  const icon = Vue.computed(() => isCopied.value ? mdi.mdiCheck : mdi.mdiContentCopy);
  const altText = Vue.computed(() => isCopied.value ? _l10n.t("Copied") : _l10n.t("Copy to clipboard"));
  async function copy() {
    if (isCopied.value) {
      return;
    }
    const value = core.toValue(content);
    try {
      await navigator.clipboard.writeText(value);
    } catch {
      prompt("", value);
    }
    isCopied.value = true;
    setTimeout(() => {
      isCopied.value = false;
    }, DELAY);
  }
  return {
    isCopied,
    copy,
    icon,
    altText
  };
}
exports.useCopy = useCopy;
//# sourceMappingURL=useCopy-3m2jDiIN.cjs.map
