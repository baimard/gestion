import { Component } from 'vue';
type SpawnDialogOptions = {
    /**
     * Container to mount the dialog to
     *
     * @default document.body
     */
    container?: Element | string;
};
export declare function spawnDialog(dialog: Component, props?: object, onClose?: (...rest: unknown[]) => void): void;
export declare function spawnDialog(dialog: Component, props?: object, options?: SpawnDialogOptions, onClose?: (...rest: unknown[]) => void): void;
export {};
